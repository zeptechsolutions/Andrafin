import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {useAuth} from '../context/AuthContext';import {ArrowRight,ShieldCheck} from 'lucide-react';
export default function Login(){const {login}=useAuth();const nav=useNavigate();const [form,setForm]=useState({email:'',password:''}),[error,setError]=useState(''),[busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{await login(form);nav('/');}catch(err){setError(err.response?.data?.message||'No se pudo iniciar sesión');}finally{setBusy(false)}};return <div className="auth-page"><section className="auth-brand"><img className="auth-logo" src="/andrafin-logo-ui.jpg" alt="AndraFin"/><div><p className="eyebrow gold">FINANZAS PERSONALES</p><h1>AndraFin</h1><p>Orden, claridad y control sobre cada movimiento de tu dinero.</p></div><div className="auth-note"><ShieldCheck size={20}/><span>Tus datos financieros permanecen asociados únicamente a tu cuenta.</span></div></section><section className="auth-panel"><div className="auth-card"><p className="eyebrow">Bienvenido</p><h2>Inicia sesión</h2><p className="muted">Accede a tu resumen financiero.</p><form onSubmit={submit} className="auth-form"><label>Correo<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="nombre@correo.com" required/></label><label>Contraseña<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Tu contraseña" required/></label><div className="forgot-row"><Link to="/forgot-password">¿Olvidaste tu contraseña?</Link></div>{error&&<p className="form-error">{error}</p>}<button className="btn primary full-btn" disabled={busy}>{busy?'Ingresando...':'Ingresar'}<ArrowRight size={18}/></button></form><p className="auth-switch">¿Primera vez en AndraFin? <Link to="/register">Crear cuenta</Link></p></div></section></div>}
